import React from 'react';
import { render, screen } from '@storybook/testing-react';
import { Button } from './index';

describe('Button', () => {
  it('renders the button', () => {
  console.log('first')
  });
});
